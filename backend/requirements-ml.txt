# ── ML / AI (training server only — NOT needed for local dev) ────
# Install with: pip install -r requirements-ml.txt
scikit-learn==1.5.0
numpy==1.26.4
pandas==2.2.2
mlflow==2.13.2
# tensorflow==2.15.0   # Install separately; large download
# tflite-runtime        # For on-device inference testing on Linux
